jdk:
  - openjdk17
env:
  global:
    - ANDROID_HOME=/usr/local/android-sdk
before_install:
  - chmod +x gradlew
  - export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
  - export PATH=$JAVA_HOME/bin:$PATH
script:
  - ./gradlew :imagepicker:publishToMavenLocal